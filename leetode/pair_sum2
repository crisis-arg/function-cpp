
#include <bits/stdc++.h>
using namespace std;
bool pair_sum(int arr[], int n, int x)
{
  map<int, bool> needed;
  for (int i = 0; i < n; i++)
  {
    int curr = arr[i];
    if (needed[curr])
    {
      return true;
    }
    needed[x - curr] = true;
  }
  return false;
}